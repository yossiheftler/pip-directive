import { YearToAgeDirective } from './year-to-age.directive';

describe('YearToAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new YearToAgeDirective();
    expect(directive).toBeTruthy();
  });
});
